apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: infraflow
  labels:
    app: web-service
spec:
  type: NodePort
  selector:
    app: web-service
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
